# Update scripts

The initial snapshot was collected from the rendered Pi package catalog:

`https://pi.dev/packages?sort=downloads`

The repository intentionally starts with the captured, auditable snapshot. The update script should be added only after the catalog parser has been tested against multiple pages and after the project decides how to preserve editorial overrides.
